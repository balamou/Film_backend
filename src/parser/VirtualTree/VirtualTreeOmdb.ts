import { VirtualTree } from './VirtualTree';
import { Omdb } from '../omdb';

export default class VirtualTreeOmdb {

    findInformation(virtualTree: VirtualTree) {
        virtualTree.forEach((season, episode) => {
            
        });
    }
}   
